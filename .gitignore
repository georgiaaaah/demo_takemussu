# Versionar SOMENTE o site (index.html + assets). Tudo mais fica de fora:
# materiais privados do cliente, artes-fonte soltas, planos internos, caches.
/*
!/index.html
!/assets/
!/README.md
!/.gitignore

# nunca versionar dentro de assets:
assets/**/*.af
assets/**/*.af~lock~
